import { dbClient } from 'crm-models'

interface Constistuent {
    id?: string
    firstName: string
    lastName: string
    email: string
    
}

export class ConstituentService {
    createUser(userData: )
}